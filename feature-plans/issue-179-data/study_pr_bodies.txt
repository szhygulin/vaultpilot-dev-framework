=== FULL `reason` text for IMPLEMENT cells (PR-correctness scoring) ===


────────────  agent-9161 (16KB) / #649  ────────────
Implemented option 3 from the issue thread: shipped the Solana / BTC / LTC cost preview block (extends EVM #636 / #648 to non-EVM chains) and split the TRON net-burn enrichment to a separate follow-up issue. New `renderNonEvmCostPreviewBlock` helper plus wiring in `collectVerificationBlocks` for Solana drafts, Solana pinned, BTC, and LTC; reuses existing envelope fee fields and DefiLlama price lookup with silent USD-degraded fallback. 196 test files / 2607 tests pass.

scope: TRON cost preview filed as a separate follow-up issue (TRON-specific post-stake net-burn enrichment); referenced descriptively in code comments since the dry-run gate returns a synthetic URL for the follow-up issue creation.

────────────  agent-9162 (32KB) / #649  ────────────
Extended the EVM cost-preview header (issue #636 / PR #648) to Solana, Bitcoin, and Litecoin. Added renderSolanaCostPreviewBlock / renderBitcoinCostPreviewBlock / renderLitecoinCostPreviewBlock funneling through a shared symbol-agnostic core, plus an injectable fetchNonEvmNativePrice opt on collectVerificationBlocks (defaults to existing DefiLlama helpers). All four chain branches now emit the cost block first, mirroring the EVM layout. TRON deferred to a follow-up issue per the user's option-3 disposition. Build clean; 2605 tests passing (12 new across 4 describe blocks).

scope: TRON cost preview deferred per option-3 in the issue thread — will need a separate issue covering feeNetSun enrichment (energy/bandwidth pool offsets after frozen-stake, fee_limit cap interaction). No envelope schema changes needed: every chain already carries its native fee on the unsigned tx (estimatedFeeLamports / decoded.feeBtc / decoded.feeLtc).

────────────  agent-9162 (32KB) / #565  ────────────
Per project-owner direction in issue #565's comment thread (overriding the earlier Markov pushback), shipped a documentation-only change to SECURITY.md adding an 'Out-of-scope: read-only data-plane integrity' section. The threat model now explicitly assumes the MCP does not fabricate read-only tool output — the project's focus is preventing the user from signing the wrong transaction. The section names the smoke-test scripts (expert-074-F, newcomer-n145-B, newcomer-n217-B), explains why bytes-level invariants and the on-device anchor don't apply, why MCP-side signing is a self-attestation antipattern, and points to out-of-band user verification as the legitimate defense. No code, no skill issue, no new tracker — consistent with the rogue-agent triage policy.

scope: Documentation-only. Build + 2593 tests pass. No skill-side companion filed — user explicitly directed 'none of the fixes to implement'.

────────────  agent-9163 (48KB) / #649  ────────────
Extended the cost preview block (#636/#648) to Solana, BTC, and LTC against existing envelope fee fields (estimatedFeeLamports / decoded.feeBtc / decoded.feeLtc), per option 3 of the issue thread. Added renderNonEvmCostPreviewBlock helper, wired it into all four user-facing emission points (Solana prepare draft, Solana pinned via previewSolanaSendHandler, Solana pinned via collectVerificationBlocks, BTC, LTC), and added a priceLookup opt for testability. TRON deliberately deferred — its post-stake net-burn math needs its own enrichment layer and was filed as a separate follow-up issue.

scope: TRON post-stake net-burn cost preview filed as a separate vaultpilot-mcp issue per option 3 of #649. Issue body drafted and submitted via gh issue create (dry-run intercepted).

────────────  agent-9163 (48KB) / #565  ────────────
Per the user's definitive directive on #565 ('none of the fixes to implement. Instead, describe this attack in security model description as not covered'), added a new 'Out-of-scope: read-only data integrity (non-signing reads)' section to SECURITY.md. The section names the threat (rogue MCP fabricating advisory-prose reads), states the assumption (MCP does not lie about read-only data), explains why every in-process mitigation collapses under the stated threat (self-attestation antipattern), distinguishes the advisory-prose surface from signing-flow reads where Inv #14 + on-device display still apply, and cites the batch-02 smoke-test scripts (expert-074-F / n145-B / n217-B) as evidence. Build clean; 2593/2593 tests pass.

scope: No skill-side companion needed — the directive is to document residual risk, not ship a defense. No cross-repo issue filed in vaultpilot-security-skill.
